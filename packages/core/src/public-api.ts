// ============================================================================
// @valfuse-node/core - Orchestration Layer
// ============================================================================
// Core coordinates between packages but contains no form domain logic.
// Form domain (schema, validation, rules, transformation) lives in @valfuse-node/form.

// TODO: Define shared utilities that both form AND localization use here.
// Currently no shared utilities identified — core is orchestration only.

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface CoreOrchestrationConfig {
  // Placeholder for future orchestration configuration
}