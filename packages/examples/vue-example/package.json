{
  "name": "@valfuse-node/example-vue",
  "version": "0.1.0",
  "private": true,
  "description": "Reference implementation and playground for valfuse-node — form validation with Vue 3",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "lint": "eslint src",
    "typecheck": "vue-tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@valfuse-node/core": "*",
    "vue": "^3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "6.0.7",
    "typescript": "6.0.3",
    "vite": "8.2.0",
    "vue-tsc": "3.3.2"
  }
}
