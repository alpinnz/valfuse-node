## 1. Informasi Umum

| Field             | Value                                 |
| ----------------- | ------------------------------------- |
| **Nama Dokumen**  | Phase 5 - Implementation Readiness v2 |
| **Nama Project**  | valfuse-node                          |
| **Modul / Fitur** | npm Conversion                        |
| **Dibuat oleh**   | Alfin Noviaji                         |
| **Dibuat pada**   | 2026-06-01                            |
| **Diubah oleh**   | Alfin Noviaji                         |
| **Diubah pada**   | 2026-06-01                            |
| **Direview oleh** | -                                     |
| **Direview pada** | -                                     |
| **Versi**         | v1                                    |
| **Status**        | draft                                 |

---

## 2. Purpose

This document provides implementation readiness for the npm conversion. **NO CODE HAS BEEN MODIFIED** — this is planning only.

---

## 3. Implementation Sequence

### Single Phase: npm Conversion

All changes are in 2 files: `package.json` and `CONTRIBUTING.md`

#### TASK-NPM-001 + TASK-NPM-002: package.json changes

- **Files affected**: `package.json`
- **Changes**:
  - `"packageManager": "yarn@1.22.22"` → `"packageManager": "npm@11.6.2"`
  - `"engines": { "yarn": ">=1.22.0" }` → `"engines": { "npm": ">=10.0.0" }`
- **Dependency rule check**: ✅ No dependency impact
- **SOLID check**: ✅ Configuration only
- **Test plan**: `npm run build` and `npm run typecheck`
- **Rollback plan**: git revert
- **Quality gates**: Build and typecheck pass

#### TASK-NPM-003: CONTRIBUTING.md update

- **Files affected**: `CONTRIBUTING.md`
- **Changes**: Replace yarn references with npm
- **Rollback plan**: git revert
- **Quality gates**: Documentation updated

---

## 4. Files Summary

### Files to MODIFY

| File              | Change                                |
| ----------------- | ------------------------------------- |
| `package.json`    | `packageManager` and `engines` fields |
| `CONTRIBUTING.md` | Replace yarn with npm in instructions |

### Files NOT affected

- All packages (form, react, vue, localization, core)
- All source code
- turbo.json
- All configs except package.json

---

## 5. Risk Assessment

| Risk                                  | Level | Mitigation                                  |
| ------------------------------------- | ----- | ------------------------------------------- |
| yarn.lock may be created accidentally | Low   | Use `npm ci` instead of `npm install` in CI |
| yarn is still installed on machine    | Low   | npm works alongside yarn, no conflict       |

---

## 6. Quality Gates

### Pre-Implementation Checklist

- [ ] Changes reviewed
- [ ] Backup of current state (git commit)

### Post-Implementation

- [ ] `npm run build` passes
- [ ] `npm run typecheck` passes
- [ ] No yarn.lock created
- [ ] CONTRIBUTING.md updated

---

## 7. Decision Summary

### Approved Changes

1. ✅ Change `packageManager` to `npm@11.6.2`
2. ✅ Change `engines.yarn` to `engines.npm: >=10.0.0`
3. ✅ Update CONTRIBUTING.md to use npm

### NOT Approved

- ❌ Any other changes (architecture is correct from v1)

---

## 8. Next Actions

**USER APPROVAL REQUIRED** before implementation begins.

Once approved:

1. Change `packageManager` in package.json
2. Change `engines` in package.json
3. Update CONTRIBUTING.md
4. Verify build passes
5. Commit with tag v0.1.1
