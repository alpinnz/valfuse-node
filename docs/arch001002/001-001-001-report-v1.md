## 1. Informasi Umum

| Field             | Value                                           |
| ----------------- | ----------------------------------------------- |
| **Nama Dokumen**  | Architecture Baseline Report v2                 |
| **Nama Project**  | valfuse-node                                    |
| **Modul / Fitur** | Monorepo Architecture Analysis - npm Conversion |
| **Dibuat oleh**   | Alfin Noviaji                                   |
| **Dibuat pada**   | 2026-06-01                                      |
| **Diubah oleh**   | Alfin Noviaji                                   |
| **Diubah pada**   | 2026-06-01                                      |
| **Direview oleh** | -                                               |
| **Direview pada** | -                                               |
| **Versi**         | v1                                              |
| **Status**        | draft                                           |

---

## 2. Executive Summary

This is a follow-up analysis after the v1 architecture refactor. The user has changed the package manager requirement from **yarn** to **npm**.

### Key Change Required

| Item           | Current                       | Required                    |
| -------------- | ----------------------------- | --------------------------- |
| packageManager | `yarn@1.22.22`                | `npm@11.x.x`                |
| engines        | `yarn: >=1.22.0`              | `npm: >=10.0.0`             |
| Lockfile       | `package-lock.json` (correct) | ✅ Already correct          |
| Scripts        | Mixed yarn/npm                | Should use npm consistently |

---

## 3. Current State Assessment

### Package Manager State

| File                            | Status                            |
| ------------------------------- | --------------------------------- |
| `package.json` `packageManager` | `yarn@1.22.22` ❌ Should be npm   |
| `package.json` `engines`        | `yarn: >=1.22.0` ❌ Should be npm |
| `package-lock.json`             | ✅ Present (npm format)           |
| `yarn.lock`                     | ✅ NOT present (good)             |

### Scripts Analysis

Most scripts use `npm run ... --workspace=...` which is correct for npm workspaces. However:

- `publish:all` uses `npm run publish:core && npm run ...` which is correct
- Example scripts use `npm run ... --workspace=...` which is correct

### Workspaces

```json
"workspaces": [
  "packages/*",
  "packages/examples/*"
]
```

Workspaces are configured correctly for npm workspaces.

---

## 4. Required Changes (Analysis Only - No Implementation)

### Critical

1. Change `packageManager` from `yarn@1.22.22` to `npm@11.x.x` or `npm@10.x.x`
2. Change `engines.yarn` to `engines.npm`
3. Update `.npmrc` if exists to use npm

### Should Check

1. Any `.claude/settings.json` that may reference yarn
2. Any `CONTRIBUTING.md` that mentions yarn
3. Any CI/CD configs that use yarn commands

### Package.json Scripts to Review

```json
"publish:core": "npm publish --workspace=packages/core",
"publish:localization": "npm publish --workspace=packages/localization",
"publish:react": "npm publish --workspace=packages/react",
"publish:vue": "npm publish --workspace=packages/vue",
```

These are already using `npm publish` ✅

---

## 5. Parallel Fan-Out Analysis

Proceeding to Phase 2 - run 9 specialist agents in parallel.

Each will analyze:

1. Their specific domain
2. Any yarn-specific references
3. NPM compliance requirements

---

## 6. User Requirements Summary

| #   | Requirement                                                                          |
| --- | ------------------------------------------------------------------------------------ |
| 1   | Schema, validateSchema, transformValues, rules in `packages/form/` ✅ (already done) |
| 2   | `packages/core` is Orchestration only ✅ (already done)                              |
| 3   | NO schema/validation/rules in core ✅ (already done)                                 |
| 4   | Shared utilities in core (used by form AND localization) - pending                   |
| 5   | Localization depends on core ✅ (already done)                                       |
| 6   | **Use npm only** ← NEW REQUIREMENT                                                   |

---

## 7. Risk Areas

| Risk                                       | Level  | Description                                        |
| ------------------------------------------ | ------ | -------------------------------------------------- |
| yarn.lock may be regenerated               | Medium | If yarn is used accidentally, it creates yarn.lock |
| CI/CD may use yarn                         | Medium | Need to verify CI configs                          |
| Developer machines may have yarn installed | Low    | npm works alongside yarn                           |

---

## 8. Next Steps

Proceed to Phase 2 — Parallel Specialist Fan-Out.
